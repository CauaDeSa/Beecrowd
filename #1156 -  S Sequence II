#include <stdio.h>

int main(){
    float x, y, sum = 0;

    x = y = 1;

    while(x < 39)
    {
        sum += x / y;

        x += 2;
        y = 2 * y;
    }

    printf("%.2f\n", sum);

    return 0;
}
