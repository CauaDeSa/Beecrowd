#include <stdio.h>
 
int main() {
 
    int value = 1, trader;
    
    while(value != 101){
    	
        trader = value % 2;
        	
        if(trader == 0)
        {
          printf("%d\n", value);
        }
        
        value++;    
    }

    return 0;
}
