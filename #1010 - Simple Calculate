#include <stdio.h>
 
int main() {
 
    int a, b, c, d;
    float e, f;
    
    scanf("%d%d%f", &a, &b, &e);
    scanf("%d%d%f", &c, &d, &f);
   
    e = b * e;
    f = d * f;
    f = e + f;
    
    printf("VALOR A PAGAR: R$ %.2f\n", f);

    return 0;
}
