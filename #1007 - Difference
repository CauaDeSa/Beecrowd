#include <stdio.h>
 
int main() {
 
    int A, B, C, D;

    scanf("%d%d%d%d", &A, &B, &C, &D);
    
    A = A * B;
    C = C * D;
    A = A - C;
    
    printf("DIFERENCA = %d\n", A);

    return 0;
}
