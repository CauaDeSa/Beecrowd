#include <stdio.h>
 
int main() {
 
    int value, rpt = 5, even = 0;
    
    while(rpt != 0){
    	
    	scanf("%d", &value);
    	
    	if((value % 2) == 0)
    		  even++;
    
    rpt--;
    
    }

	  printf("%d valores pares\n", even);

    return 0;
}
