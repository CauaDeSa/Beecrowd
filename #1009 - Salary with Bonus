#include <stdio.h>
 
int main() {
 
    char name[20];
    double sal, total, aument;
    
    scanf("%s", &name);
    scanf("%lf%lf", &sal, &total);
    
    aument = total * 0.15;
    sal = aument + sal;
    
    printf("TOTAL = R$ %.2lf\n", sal);

    return 0;
}
