#include <stdio.h>
 
int factorial(int number);

int main(){
    int number;

    scanf("%d", &number);    

    printf("%d\n", factorial(number));

    return 0;
}

int factorial(int number){
    if(number == 1)
        return 1;

    return factorial(number - 1) * number;
}
