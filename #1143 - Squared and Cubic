#include <stdio.h>
#include <math.h>
 
int main(){
    int n;

    scanf("%d", &n);

    for (int i = 1; i < n + 1; i++)
    {
        printf("%d %.0lf %.0lf\n", i, pow(i, 2), pow(i, 3));
    }
    
    return 0;
}
