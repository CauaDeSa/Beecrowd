#include <stdio.h>
 
int main() {
 
    int num;
    float hours, perhour;
    
    scanf("%d%f%f", &num, &hours, &perhour);
    
    perhour = perhour * hours;
    
    printf("NUMBER = %d\n", num);
    printf("SALARY = U$ %.2f\n", perhour);
 
    return 0;
}
