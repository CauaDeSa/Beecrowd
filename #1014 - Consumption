#include <stdio.h>
 
float divisao(int x, float y);

int main() {
 
    int x;
    float y, kml;
    
    scanf("%d%f", &x, &y);
    
    kml = divisao(x, y);
 
    printf("%.3f km/l\n", kml);
    
    return 0;
}

float divisao(int x, float y){
    return x / y;
}

